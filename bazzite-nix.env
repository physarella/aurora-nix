# Here we set a bunch of parameters for your image build

# output image name, usually same as repo name, change as needed
IMAGE_NAME=aurora-nix

# Make this the same as your github username/organisation
# Case is irrelevant here: build.yml lowercases both this and IMAGE_NAME before
# pushing, because ghcr.io paths must be lowercase.
REPO_ORGANIZATION="physarella"

IMAGE_DESC="Bazzite with Nix as the package manager, no distrobox and no Homebrew"
IMAGE_KEYWORDS="bootc,oci,linux,aurora,nix,development"

# Put your own image here for a fancy profile on https://artifacthub.io/!
IMAGE_LOGO_URL="https://avatars.githubusercontent.com/u/120078124?s=200&v=4"
DEFAULT_TAG="latest"
BIB_IMAGE="quay.io/centos-bootc/bootc-image-builder:latest"
